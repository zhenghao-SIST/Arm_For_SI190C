cmake_minimum_required(VERSION 3.8)
project(joint_kinematics_node)

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# find dependencies
find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(Eigen3 REQUIRED)
find_package(geometry_msgs REQUIRED)

include_directories(include)

add_library(dh_server
    src/DH_server.cpp
)
target_link_libraries(dh_server Eigen3::Eigen)
add_library(utility
    src/utility.cpp
)

add_executable(joint_kinematics_node
  src/joint_kinematics_node.cpp
)
target_link_libraries(joint_kinematics_node dh_server utility Eigen3::Eigen)

ament_target_dependencies(joint_kinematics_node
  rclcpp
  sensor_msgs
  geometry_msgs
)

add_executable(pose_publisher test/pose_publisher.cpp)
ament_target_dependencies(pose_publisher rclcpp geometry_msgs)

add_executable(joint_publisher test/joint_publisher.cpp)
ament_target_dependencies(joint_publisher rclcpp sensor_msgs)

add_executable(joint_range_test test/joint_range_test.cpp)
target_link_libraries(joint_range_test utility)
ament_target_dependencies(joint_range_test rclcpp sensor_msgs)

add_executable(fk_subscriber test/fk_subscriber.cpp)
ament_target_dependencies(fk_subscriber rclcpp geometry_msgs)

install(TARGETS
  joint_kinematics_node
  pose_publisher
  joint_publisher
  joint_range_test
  fk_subscriber
  DESTINATION lib/${PROJECT_NAME}
)

install(
  DIRECTORY
  launch config scripts
  DESTINATION share/${PROJECT_NAME}
)

if(BUILD_TESTING)
  find_package(ament_lint_auto REQUIRED)
  # the following line skips the linter which checks for copyrights
  # comment the line when a copyright and license is added to all source files
  set(ament_cmake_copyright_FOUND TRUE)
  # the following line skips cpplint (only works in a git repo)
  # comment the line when this package is in a git repo and when
  # a copyright and license is added to all source files
  set(ament_cmake_cpplint_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()
endif()

ament_package()
